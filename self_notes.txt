C++ is used for developing high performace application , games , sys software

fast execution spped and direct memory management capabilities
widely used in OS , competitive programming

-------------------

header file : #include<iostream> , adds input/output , common headers fstream (files) vector(stl) , bits/stdc++.h

namespace declaration : allow direct use of standard name cout without std::

main function : programs entry point , execution starts here and returns an integer with return 0 means successful execution

return : return 0 , statement terminates mains() function and indicates that program executed successfully

Input and Output operations 
cin>>
cout<<

any space in the input means the input is ended
e.g like if cin>>tripti pandey
it means if cout<< is done then only tripti is printed

----------------------------------------

Identifiers

user defined names , represent program elemets such as variables , function , classes and objects

inshort indentifiers means name
class Identifiers
method Identifiers
variable Identifiers
object Identifiers

Rules for Naming Identifiers
A-Z, or a-z, 0-9, _
special char and spaces not allowed

only begin with letter or _

reserved keywords cannot be used as identifier 

unique within scope or namespace 

case sensitive

-----------------------------

keywords

reserved words with special meaning
if keyword mistakenly used as variable name ,then code will not compile

Categories of C++ keywords
Data types : bool , char , int , long , short , signed , unsigned , float , double , void

Control flow : if , else , switch , default , for , while , do , break , continue, goto

boolean and null : true , false , nullptr

memory management : new , delete , sizeof , alignas 

classes and structs : class , union , enum , mutable this

access specifiers : public , private , protected

functions and modifiers : inline , explicit , virtual , overrise , final, typedef

-------------------------------

DATA TYPES in C++

form of data representation , defines the kind of value it can store 
data types determine memory allocation 

Types of data types

Basic data types

int :  stores integer value int n =100; 4 bytes  

float : stores decimal numbers using float keyword , 4 bytes ,  stores 1.2e-38 to 3.4 e+38 float f =36.5f;

double : store decimal numbers with higher precision than float , 8 bytes , double pi =3.14817672798465

char : stores single char enclosed in single quotes , 1 byte , ASCII value e.g char c = 'A';

bool : stores logical value , true(1) or false(0) , size 1 byte  e.g bool isTrue = true;

void : represent absence of value , not to create a variable of void type , it is used for pointer and functions ,that does not return any value using keyword void 



Derieved data types

array : stores multiple values of same type in contiguous memory locations

pointer : stores memory address of another variable 

reference : it acts as alias for exixting variable 

function : used to represent a derieved type because they have specific return type and parameter types


User Defined data types

class : it combines data functions into single unit

structure : used to groups related variables under one name 

union : allows multiple members to share same memory location

typedef
using 

SIZE OF OPERATOR

cout<<sizeof(int);

Data Type Modifiers 

It can alter the size , range or representation of certain fundamental data types
commonly modifiers are 

short : smaller integer types supported
long : integer type with least as much storage as int
signed : allows integer to represent both positive and negative values
unsigned : integer type of non negative values are provided

---------------------------------------------------------------

OPERATOR 

Symbols that operate on values to perfrm specific mathemcatical or logical computation on given values

int a = 10 + 20;

6 types of Operators 

Arithmetic Operator

Additon +
substraction -
multiplication *
division /

modulo % gives reminder also when division by zero gives compile time error

increment ++
decrement --

Relational Operators

comparision of values of 2 operands 
== equals to
> , >=
< ,<=
!=

Logical Operator

Logical AND , logical OR , logical NOT 

a && b
a || b
!b

Bitwise Operators

Binary AND &
Binary OR |
Binary XOR ^
Left Shift << shift the value to left by the number of bits specified
Right Shift >> shift the value to right by number of bits specified
One's Complement ~ changes digit 1 to 0 and 0 to 1

a & b
a | b
a ^ b
a << 1
a >> 1
~(a)


ASSIGNMENT OPERATOR

= , += , -= , *= , /=

a += b;
a *= b;
a /= b;


Ternary or Conditional Operators


Return value based on the condition , 3 operands are used therefore its called ternary 
expression 1 ? expression 2 : expression 3

int result = (a <b )?b:a;


MISCELLANEOUS Operators

sizeof(char);
sizeof(var_name);

Comma Operator
 ,

 addressof Operator ( & )
 is used to find the memory address in which a particular variable is stored , it is also used to create a refrenece

 Dot operator ( . ) is used to access members of structure variables or class objects using their object names

 Arroe operator -> is used to access variables of classes or structures throught its pointer


TYPE CASTING OPERATOR

char c = 'a';
int i = static_cast<int>(c);

DECISION MAKING in C++

it is about conditional execution of part of code 

if else 

if(conditon){ }

if else

if (condition) { }
else { }

if else if

if (condition 1) { }
else if (condition 2) { }
else { }

nested if 

if(condition 1)
 {
  if (confition){ }
  else { }
  }
else {
  if(condition) {

  }
  else { }
}

SWITCH  

switch expression 
{
  case 1;
  break;

  case 2;
  break;

  case 3;
  break;

  default;
}

LOOPS 

FOR LOOPS

for (initialisation ; condition , updation){
  //body of loop;
}

WHILE LOOPS

while (condition)
{
  //body of loop;
  //update expression;
}

DO WHILE LOOP

do {
  //body of the loop
  // update expression
}while (condition);

FOR EACH LOOP

is range based for loop , automatically iterates over each element of a container or array using containers begin() and end() function

Use of reference v/s value 

by value : for(auto it : arr) , works on a copy , modifications wont affect the original

by reference : for (auto &iy : arr) , you can modify elements directly

INFINITE LOOPS

it is endless loop with functional exit that repeats indefinitely 

NESTING OF LOOPS 

one loop inside another , inner loop is executed completely for each iteration of outer loop 

iteration over 2d array

BREAK OUT LOOP

Break statement when encountered , terminates the loop regardless of test condition 

CONTINUE statement

skips particular iteration 
skips the remaining code inside current iteration of loop and proceeds to next iteration 

FUNCTION  
a reusable block of code that performs specific task
divide program into smaller logical units ,improve readability and makes code easier

it accepts parameters , execute statements , return value


FUNCTION SYNTAX

return_type function_name (parameter_list)
{
  //function body
  return expression;
}

Return type : specifies type of return , if there is no return value then use void

function name : use to call the function

parameter list : inputs that function accept , it can empty if no input is needed

funtion body : the block of code that return funtion when it is called

FUNCTION DECLARATION v/s DEFINITION

declaration introduces a function to the compiler by specifying its  return type , name and parameters without the body and is used when the function is defined later in another file
e.g int add(int , int);

definition contains the actual code that specifies what the function does when it is called
e.g 
int add(int a , int b)
{
  return a+b;
}

function must be declared before it is used so the compiler knows its name , return type and parameters ,
this can be done either by defining the function before main() or by declaring it using a function prototype if the definition appears later

CALLING A FUNCTION

function is used by calling its name followed by paranthese, passing required arguments if any , which executes the code inside the function

e.g 

greet() function : parameterless void function that prints a welcome message and is called in main() using greet();

multiply(int a, int b) function : takes 2 intgers , returns their product and is called in main() with 4 and 5 storing the result which is then printed

PARAMETERS OR ARGUMENTS 

Function can accept input values called arguments , which are passed during the function call and received through types 

parameters defined inside the function parantheses 

e.g return_type name(type 1 name 1 , type2 name2)
{
  //function body
  return val;
}


TYPES OF FUNCTIONS IN C++

Based on origin

Library functions :
built in functions provided by c++ standard libraries , such as sqrt() , abs() , getline() 
it can be used by including appropriate header like <iostream> ,<cmath> , <string>

User defined functions :

created by the programmer to perform specific tasks in the program

Based on input and return type

No parameters , no return value :  the function performs a task but does not take input or return anything

Parameters , no return value : the function takes input but does not return a result

no parameters , return value : the function returns a result but does not take any input

parameters and return value : the function takes input and returns a result


FUNCTION OVERLOADING 

allows us to define multiple functions with the same name but with different parameters , so the same function name can perform different tasks depending on the values and types of arguments passed

Avoids name conflicts and unnecessary function names 
makes the code easy to understand and makeing user friendly as the same meaningful function name is used for diff types of inputs

asssume that u have to add 2 integers it is easy to make a function for that 

int add2(int a , int b)
{
  return a+b;
}

now assume that u also need a function that adds 3 integers , once method is to create another function with diff name

int add3(int a , int b , int c)
{
  return a+b+c
}


but it may be diffi ult for u to understand the behaviour of funtion because its name differs , moreover it is very possible to forget about other function

this is where function overloading comes into play , it allows us to create both of the functions with same name as shown 

#include <iostream>
using namespace std;

int add(int a, int b) {
    return a + b;
}
// Same function with different arguments
int add(int a, int b, int c) {
    return a + b + c;
}

int main() {
    int a = 5, b =7, c = 11;
    // Add function to add two numbers
    cout << add(a, b) << endl;
    // Add function to add three numbers
    cout << add(a, b, c);
    return 0;
}


DIFFERENT WAYS OF FUNCTION OVERLOADING

3 ways of function overloading are:

by having diff number of parameters 
by havinf diff types of parametersby having both diff number and and types of parameters

Different number of parameters

we can overload a function by changing the number of parameters that it accepts














